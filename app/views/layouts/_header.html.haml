#navigation
  %ul
    - if user_signed_in?
      %li= image_tag('guest-12.png')
      %li= "Hola #{current_user.username}!"
      %li= link_to 'Perfil', edit_user_registration_url
      %li= link_to 'Salir', destroy_user_session_path
    - else
      %li= link_to 'Crear cuenta', new_user_registration_path
      %li= link_to 'Acceder', new_user_session_path

%header
  #logo
    = link_to image_tag('logo2.png'), root_url

  %ul#main-menu
    %li= link_to 'Comunidad', '/community'
    %li= link_to 'Fragmentos R', snippets_url
    %li= link_to 'Ofertas de trabajo', job_offers_url
    - if can? :manage, User
      %li= link_to 'Gestión de usuarios', members_url
