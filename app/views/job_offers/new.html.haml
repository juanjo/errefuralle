- content_for :title, "Añadir oferta de trabajo"

#conmenu
  %h1 Añadir nueva oferta de trabajo
  #actions
    = link_to 'Volver a ofertas de trabajo', job_offers_url, :class => 'button'

= form_for @job_offer do |f|
  = render :partial => 'form', :locals => { :f => f }
  #buttons
    = f.submit 'Añadir oferta de trabajo'
